Notas:
    - 5 capas. 32 -> 64 -> 128 -> 256 -> 512 + MLP (512 - 1)
    - decay=lr/epochs

Parámetros:
    data_augmentation = False
    batch_size = 64
    lr = 0.01
    dropout = True
    epochs = 15
    optimizer = "SGD"

Análisis de resultados:
    - Hemos ampliado limitado algo el problema de inestabilidad de accuracy de validation. Sigue existiendo un pequeño
    problema de overfitting que se ampliará al aumentar los epochs. Pese a ello, dado que se trata de la red sin data
    augmentation se observan mejoras.

Ideas de mejora:
    - Regularizers
