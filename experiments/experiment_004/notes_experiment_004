Notas:
- Hemos añadido data augmentation sencilla debido al overfitting. Rotation simple, zooms i shifts.


Parámetros:
- En la función load_dataset() pasamos un parametro bool (data_augmentation). Si true, aumentamos datos.


Análisis de resultados:
Ya no se produce overfitting, de hecho se produce underfitting y por lo tanto se decide probar con una arquitectura más compleja (mayo número de layers). Por supuesto además debemos entrenar durante más epochs.

Ideas de mejora:
- Regularizers --> L2